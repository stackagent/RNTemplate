
export const initialState = {

}

export function globalReducer(state = initialState, action) {
    switch (action.type) {
        // case chartConsts.ITEMS + promiseConsts.FULFILLED:
        //     state = { ...state, items: action.payload.data }
        //     break;

        default:
            break;
    }
    return state;
}